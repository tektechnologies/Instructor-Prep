<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>List</title>
</head>
<body>
  <h1>List</h1>
  <!-- this will output the grocery list -->
  <ul>
    <!-- we need the EJS open and close tags to start stop the js and allow us to enter the html  -->
    <% for(let i = 0; i < theListKeyName.length; i++){ %>
<!-- The equals will show on the page and the removal of the equals will hide the statement  -->
      <li>Item: <%= theListKeyName[i].product %> X - <%=theListKeyName[i].quantity %></li>

    <% } %>

  </ul>
<!-- This will out put the goonies  -->
  <ul>
    <% for(let i = 0; i < names.length; i++){ %>

        <li> <%= names[i] %> </li>

    <% } %>
  </ul>
<!-- this shows up in the terminal  -->
  <% console.log('from ejs', names); %>


  <!-- this is to show the forEach and the list Name:  -->
  <ul>
    <% names.forEach(name => { %>
      <li>Name : <%= name %> </li>
    <% }); %> 
  </ul>
</body>
</html>